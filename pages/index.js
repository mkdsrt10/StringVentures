import Head from 'next/head'
import styles from '../styles/Home.module.css'
import Navbar from "../src/Navbar";
import Footer from "../src/Footer";
import Impact from "../src/Impact";
import Investment from "../src/InvestmentPloicy";
import Team from "../src/Team";

export default function Home({resource}) {
    function clickhandler(){

    }
  return (
    <div className={styles.Root}>
        <Head>
            <title>Create Next App</title>
            <meta name="description" content="Generated by create next app" />
            <link rel="icon" href="/favicon.ico" />
        </Head>
        <div className={styles.mainContainer}>
            <div className={styles.top_contianer}>
                <Navbar clickhandler={clickhandler}/>
                <div className={styles.top_contianer_content}>
                    <div className={styles.hero_subhead}>
                        STARTUP STUDIO AND EARLY STAGE VENTURE CAPITAL FUND
                    </div>
                    <div className={styles.hero_heading}>
                        We build the future with you
                    </div>
                </div>
            </div>
            <Investment />
            <Impact />
            <div className={styles.question}>
                <div className={styles.hero_subhead_question}>
                    EARLY STAGE VENTURE CAPITAL FUNDING
                </div>
                <div className={styles.hero_heading_question}>
                    Are you working on <span>early-stage startup</span>?
                </div>
                <div className={styles.partner_button}>
                    Let's talk
                </div>
            </div>
            <Team />
            {/*<Testimonial />*/}
            {/*<Resources resource={resource} />*/}
            {/*<WorkTogether clickhandler={clickhandler} />*/}
        </div>
        <Footer />
    </div>
  )
}

export async function getStaticProps({ params }) {

    // const res = await fetch(`https://.../data`)
    // const data = await res.json()

    return {
        props: {resource:[]},
        // Re-generate the post at most once per second
        // if a request comes in
        revalidate: 5,
    }
}
